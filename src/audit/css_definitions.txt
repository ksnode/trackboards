=== PLIK: shared.module.css ===
.actionsRow {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  flex-wrap: wrap;
}

/* ── Shared styles for Profile & Admin pages ── */

/* ── Breadcrumb ── */
.breadcrumb {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  font-size: var(--font-size-sm);
  color: var(--color-text-tertiary);
  margin-bottom: var(--space-4);
}

.breadcrumbCurrent {
  color: var(--color-text-primary);
  font-weight: 500;
}

.breadcrumbLink {
  color: var(--color-text-secondary);
  text-decoration: none;
  transition: color var(--transition-fast);
}

.breadcrumbLink:hover {
  color: var(--color-accent);
}

.breadcrumbSep {
  color: var(--color-text-tertiary);
  user-select: none;
}

/* ── Buttons ── */
.btnDanger {
  padding: 2px var(--space-2);
  font-size: var(--font-size-xs);
  border-radius: var(--border-radius-md);
  border: 1px solid var(--color-danger);
  background: transparent;
  color: var(--color-danger);
  cursor: pointer;
  transition: all var(--transition-fast);
  white-space: nowrap;
}

.btnDanger:hover {
  background: var(--color-danger);
  color: white;
}

.btnGhost {
  padding: 2px var(--space-2);
  font-size: var(--font-size-xs);
  border-radius: var(--border-radius-md);
  border: 1px solid var(--color-border-primary);
  background: transparent;
  color: var(--color-text-secondary);
  cursor: pointer;
  min-width: 85px;
  transition: all var(--transition-fast);
  white-space: nowrap;
}

.btnGhost:hover {
  border-color: var(--color-primary);
  background: var(--color-bg-tertiary);
  color: var(--color-text-primary);
}

.btnGhostDanger {
  composes: btnGhost;
  color: var(--color-danger);
}

.btnGhostDanger:hover {
  border-color: var(--color-danger);
  background: var(--color-bg-tertiary);
  color: var(--color-danger);
}

.btnGhostInfo {
  composes: btnGhost;
  color: var(--color-info);
}

.btnGhostInfo:hover {
  border-color: var(--color-info);
  background: var(--color-bg-tertiary);
  color: var(--color-info);
}

.chevronOpen {
  transform: rotate(180deg);
  transition: transform var(--transition-fast);
}

/* ── Color dot ── */
.colorDot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  flex-shrink: 0;
}

/* ── Empty + Loading ── */
.emptyState {
  text-align: center;
  padding: var(--space-8) var(--space-6);
  color: var(--color-text-secondary);
  font-size: var(--font-size-sm);
}

/* ── Filter row ── */
.filterRow {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  margin-bottom: var(--space-4);
  flex-wrap: wrap;
}

.loading {
  padding: var(--space-8) var(--space-6);
  text-align: center;
  color: var(--color-text-secondary);
}

.navCard {
  display: block;
  padding: var(--space-6);
  border: 1px solid var(--color-border-primary);
  border-radius: var(--border-radius-lg);
  background: var(--color-bg-secondary);
  text-decoration: none;
  color: inherit;
  transition: border-color var(--transition-fast), background var(--transition-fast);
}

.navCard:hover {
  border-color: var(--color-accent);
  background: var(--color-bg-tertiary);
}

.navCardDanger {
  display: block;
  padding: var(--space-6);
  border: 1px solid var(--color-border-danger);
  border-radius: var(--border-radius-lg);
  background: var(--color-bg-secondary);
  text-decoration: none;
  color: inherit;
  transition: border-color var(--transition-fast), background var(--transition-fast);
}

.navCardDanger:hover {
  border-color: var(--color-danger);
  background: var(--color-background-danger);
}

.navCardDesc {
  font-size: var(--font-size-sm);
  color: var(--color-text-secondary);
}

.navCardTitle {
  font-size: var(--font-size-base);
  font-weight: 600;
  color: var(--color-text-primary);
  margin-bottom: var(--space-1);
}

/* ── Navigation cards ── */
.navCards {
  display: flex;
  flex-direction: column;
  gap: var(--space-4);
  max-width: 50%;
}

@media (max-width: 600px) {
  .navCards {
    max-width: 100%;
  }
}

/* ── Portal dropdown ── */
.optionsDropdownPortal {
  position: fixed;
  min-width: 150px;
  background: var(--color-bg-secondary);
  border: 1px solid var(--color-border-primary);
  border-radius: var(--border-radius-md);
  box-shadow: var(--shadow-md);
  z-index: 9999;
  overflow: hidden;
}

/* ── Options dropdown ── */
.optionsItem {
  display: block;
  width: 100%;
  text-align: left;
  padding: var(--space-2) var(--space-3);
  font-size: var(--font-size-sm);
  color: var(--color-text-secondary);
  background: transparent;
  border: none;
  cursor: pointer;
  transition: background var(--transition-fast);
  white-space: nowrap;
}

.optionsItem:hover {
  background: var(--color-bg-tertiary);
  color: var(--color-text-primary);
}

.optionsItemDanger {
  display: block;
  width: 100%;
  text-align: left;
  padding: var(--space-2) var(--space-3);
  font-size: var(--font-size-sm);
  color: var(--color-text-danger);
  background: transparent;
  border: none;
  cursor: pointer;
  transition: background var(--transition-fast);
  white-space: nowrap;
}

.optionsItemDanger:hover {
  background: var(--color-background-danger);
}

.optionsSep {
  border-top: 1px solid var(--color-border-primary);
  margin: 2px 0;
}

.previewActions {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  flex-shrink: 0;
}

.previewBody {
  flex: 1;
  overflow-y: auto;
  padding: var(--space-4);
}

.previewHeader {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--space-3) var(--space-4);
  border-bottom: 1px solid var(--color-border-primary);
  flex-shrink: 0;
}

.previewModal {
  background: var(--color-bg);
  border: 1px solid var(--color-border-primary);
  border-radius: var(--border-radius-lg);
  width: 90vw;
  max-width: 900px;
  max-height: 85vh;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

/* ── Board preview modal ── */
.previewOverlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.6);
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: center;
}

.previewTitle {
  font-size: var(--font-size-base);
  font-weight: 600;
  color: var(--color-text-primary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* ── Page Root Layout ── */
.root {
  max-width: var(--content-max-width);
  padding: var(--content-padding-y) var(--content-padding-x);
  width: 100%;
}

.shareModeMenuItem {
  display: flex;
  align-items: center;
  gap: 5px;
  width: 100%;
  padding: var(--space-1) var(--space-2);
  font-size: var(--font-size-xs);
  background: transparent;
  color: var(--color-text-secondary);
  border: none;
  cursor: pointer;
  min-width: 85px;
  text-align: left;
  transition: background var(--transition-fast);
}

.shareModeMenuItem:hover {
  background: var(--color-bg-tertiary);
  color: var(--color-text-primary);
}

.shareModeMenuItemActive {
  display: flex;
  align-items: center;
  gap: 5px;
  width: 100%;
  padding: var(--space-1) var(--space-2);
  font-size: var(--font-size-xs);
  background: var(--color-accent);
  color: white;
  border: none;
  cursor: pointer;
  text-align: left;
}

.shareModeMenuItemActive:hover {
  background: var(--color-accent-hover);
}

/* ── Share mode dropdown ── */
.shareModeTogglePrivate {
  display: inline-flex;
  align-items: center;
  gap: 3px;
  padding: 2px var(--space-2);
  font-size: var(--font-size-xs);
  border: 1px solid var(--color-border-primary);
  border-radius: var(--border-radius-md);
  background: transparent;
  color: var(--color-text-secondary);
  cursor: pointer;
  transition: all var(--transition-fast);
  white-space: nowrap;
  min-width: 85px;
  justify-content: space-between;
}

.shareModeTogglePrivate:hover {
  border-color: var(--color-primary);
  background: var(--color-bg-tertiary);
  color: var(--color-text-primary);
}

.shareModeTogglePublic {
  display: inline-flex;
  align-items: center;
  gap: 3px;
  padding: 2px var(--space-2);
  font-size: var(--font-size-xs);
  border: 1px solid var(--color-border-primary);
  border-radius: var(--border-radius-md);
  background: var(--color-background-info);
  color: var(--color-text-secondary);
  cursor: pointer;
  transition: all var(--transition-fast);
  white-space: nowrap;
  min-width: 85px;
  justify-content: space-between;
}

.shareModeTogglePublic:hover {
  border-color: var(--color-primary);
  color: var(--color-text-info);
}

/* ── Table layout ── */
.table {
  width: 100%;
  border-collapse: separate;
  border-spacing: 0;
}

.tableCell {
  padding: var(--space-3);
  font-size: var(--font-size-sm);
  color: var(--color-text-primary);
  border-bottom: 1px solid var(--color-border-tertiary);
  vertical-align: middle;
}

.tableCellActions {
  padding: var(--space-2) var(--space-3);
  border-bottom: 1px solid var(--color-border-tertiary);
  vertical-align: middle;
}

.tableCellMono {
  font-family: var(--font-mono);
  font-size: var(--font-size-xs);
  color: var(--color-text-secondary);
}

.tableHead {
  position: sticky;
  top: 0;
}

.tableHeadCell {
  text-align: left;
  font-size: var(--font-size-xs);
  font-weight: 500;
  color: var(--color-text-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.04em;
  padding: var(--space-2) var(--space-3);
  border-bottom: 1px solid var(--color-border-primary);
  background: var(--color-bg);
  white-space: nowrap;
}

.tableRow {
  transition: background var(--transition-fast);
}

.tableRow:hover {
  background: var(--color-bg-secondary);
}

/* ── Table title truncation ── */
.titleTruncate {
  display: inline-block;
  max-width: 240px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  vertical-align: middle;
}

.userPickerEmpty {
  padding: var(--space-3);
  font-size: var(--font-size-xs);
  color: var(--color-text-tertiary);
  text-align: center;
}

.userPickerItem {
  display: block;
  width: 100%;
  text-align: left;
  padding: var(--space-2) var(--space-3);
  font-size: var(--font-size-sm);
  color: var(--color-text-primary);
  background: transparent;
  border: none;
  cursor: pointer;
  transition: background var(--transition-fast);
}

.userPickerItem:hover {
  background: var(--color-bg-tertiary);
  color: var(--color-accent);
}

/* ── User picker ── */
.userPickerSearch {
  width: 100%;
  padding: var(--space-2) var(--space-3);
  border: none;
  border-bottom: 1px solid var(--color-border-primary);
  background: var(--color-bg);
  color: var(--color-text-primary);
  font-size: var(--font-size-sm);
  outline: none;
  font-family: var(--font-ui);
}

.userPickerSearch:focus {
  border-bottom-color: var(--color-accent);
}

=== PLIK: admin.module.css ===
/* ── Admin page styles ── */

/* ── Actions panel ── */
.actionsPanel {
  display: flex;
  align-items: stretch;
  gap: var(--space-2);
}

.actionsPanelGroup {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.actionsPanelSep {
  width: 1px;
  background: var(--color-border-primary);
  align-self: stretch;
  flex-shrink: 0;
}

.btnDangerFixed {
  composes: btnDanger from './shared.module.css';
  min-width: 75px;
  text-align: center;
  justify-content: center;
}

.btnDisabled {
  padding: 2px var(--space-2);
  font-size: var(--font-size-xs);
  border-radius: var(--border-radius-md);
  border: 1px solid var(--color-border-primary);
  background: var(--color-bg-tertiary);
  color: var(--color-text-tertiary);
  cursor: not-allowed;
  white-space: nowrap;
  opacity: 0.6;
}

.btnDisabled:hover {
  border-color: var(--color-primary);
  background: var(--color-bg-tertiary);
}

/* ── Fixed-width action buttons ── */
.btnGhostFixed {
  composes: btnGhost from './shared.module.css';
  min-width: 75px;
  text-align: center;
  justify-content: center;
}

.btnInfoFixed {
  padding: 2px var(--space-2);
  font-size: var(--font-size-xs);
  border-radius: var(--border-radius-md);
  border: 1px solid var(--color-text-info);
  background: transparent;
  color: var(--color-text-info);
  cursor: pointer;
  transition: all var(--transition-fast);
  white-space: nowrap;
  min-width: 75px;
  text-align: center;
  justify-content: center;
}

.btnInfoFixed:hover {
  background: var(--color-info);
  color: white;
}

.filterCheckLabel {
  display: flex;
  align-items: center;
  gap: var(--space-1);
  font-size: var(--font-size-xs);
  color: var(--color-text-secondary);
  cursor: pointer;
  user-select: none;
}

.filterCheckLabel input[type="checkbox"] {
  accent-color: var(--color-accent);
  cursor: pointer;
}

/* ── Filter check options ── */
.filterChecks {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  flex-wrap: wrap;
}

/* ── Search input ── */
.searchInput {
  width: 100%;
  max-width: 320px;
  padding: var(--space-2) var(--space-3);
  font-size: var(--font-size-sm);
  font-family: var(--font-ui);
  color: var(--color-text-primary);
  background: var(--color-bg-secondary);
  border: 1px solid var(--color-border-primary);
  border-radius: var(--border-radius-md);
  outline: none;
  transition: border-color var(--transition-fast);
  margin-bottom: var(--space-4);
}

.searchInput:focus {
  border-color: var(--color-accent);
}

.searchInput::placeholder {
  color: var(--color-text-tertiary);
}

/* ── Base toggle button ── */
.toggleBtn {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  min-width: 75px;
  justify-content: space-between;
  padding: 2px var(--space-2);
  font-size: var(--font-size-xs);
  border: 1px solid var(--color-border-primary);
  border-radius: var(--border-radius-md);
  background: var(--color-bg-tertiary);
  color: var(--color-text-secondary);
  cursor: pointer;
  transition: all var(--transition-fast);
  white-space: nowrap;
  font-weight: 500;
}

.toggleBtn:hover:not(:disabled) {
  border-color: var(--color-primary);
}

.toggleBtn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.toggleBtnInfo {
  background: var(--color-background-info);
  color: var(--color-info);
}

.toggleBtnInfo:hover:not(:disabled) {
  border-color: var(--color-info);
}

=== PLIK: profile.module.css ===
.accountDetails {
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 0;
}

.accountEmail {
  font-size: var(--font-size-base);
  font-weight: 500;
  color: var(--color-text-primary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.accountMeta {
  font-size: var(--font-size-xs);
  color: var(--color-text-tertiary);
  display: flex;
  align-items: center;
  gap: var(--space-3);
  flex-wrap: wrap;
}

/* ── Profile page styles ── */

/* ── Account section ── */
.accountSection {
  display: flex;
  align-items: center;
  gap: var(--space-4);
  padding: var(--space-6);
  border: 1px solid var(--color-border-primary);
  border-radius: var(--border-radius-lg);
  background: var(--color-bg-secondary);
  margin-bottom: var(--space-4);
}

.avatar {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  background: var(--color-accent);
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--font-size-xl);
  font-weight: 400;
  text-transform: uppercase;
  flex-shrink: 0;
  user-select: none;
}

/* ── Warning button ── */
.btnWarning {
  padding: 2px var(--space-2);
  font-size: var(--font-size-xs);
  border-radius: var(--border-radius-md);
  border: 1px solid var(--color-text-warning);
  background: transparent;
  color: var(--color-text-warning);
  cursor: pointer;
  transition: all var(--transition-fast);
  white-space: nowrap;
}

.btnWarning:hover {
  background: var(--color-warning);
  color: white;
}

/* ── Manage page input ── */
.manageInput {
  width: 100%;
  max-width: 320px;
  padding: var(--space-2) var(--space-3);
  border: 1px solid var(--color-border-primary);
  border-radius: var(--border-radius-md);
  background: var(--color-bg);
  color: var(--color-text-primary);
  font-size: var(--font-size-sm);
  margin-bottom: var(--space-4);
  outline: none;
  font-family: var(--font-ui);
}

.manageInput:focus {
  border-color: var(--color-danger);
}

.roleBadge {
  font-size: var(--font-size-xs);
  font-weight: 600;
  padding: 1px var(--space-2);
  border-radius: var(--border-radius-sm);
  background: var(--color-bg-tertiary);
  color: var(--color-text-secondary);
  text-transform: uppercase;
  letter-spacing: 0.03em;
}

.roleBadgeAdmin {
  background: var(--color-accent);
  color: white;
}
